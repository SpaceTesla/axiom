apiVersion: v1
kind: Service
metadata:
  name: axiom-api-service
  labels:
    app: axiom-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: axiom-api

