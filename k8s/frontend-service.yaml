apiVersion: v1
kind: Service
metadata:
  name: axiom-frontend-service
  labels:
    app: axiom-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8501
    protocol: TCP
    name: http
  selector:
    app: axiom-frontend

